import { Router } from 'express';

const apiRouter = Router();

// Define your routes here
// e.g., apiRouter.get('/example', (req, res) => { ... });

export default apiRouter; // Ensure you export apiRouter
